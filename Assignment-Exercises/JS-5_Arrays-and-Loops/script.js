// Step 1: Create an array named `favoriteMovies`
let favoriteMovies = ["Inception", "Hunger Games", "Divergent", "Money Heist"];

// Step 2: Use a loop to iterate through the `favoriteMovies` array
for (let i = 0; i < favoriteMovies.length; i++) {
    // Log each movie to the console
    console.log(favoriteMovies[i]);
}
